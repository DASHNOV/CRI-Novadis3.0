# Copier ce fichier en .env sur le VPS et remplir les valeurs
# cp .env.production.example .env

# PostgreSQL
DB_NAME=cri_novadis
DB_USER=cri_user
DB_PASSWORD=remplacer_par_mot_de_passe_fort

# JWT — générer avec : openssl rand -base64 64
JWT_SECRET=remplacer_par_cle_aleatoire_64_caracteres_minimum

# Email (SMTP)
EMAIL_SMTP_HOST=smtp.office365.com
EMAIL_USERNAME=noreply@novadis.eu
EMAIL_PASSWORD=remplacer_par_mot_de_passe_smtp
EMAIL_FROM=noreply@novadis.eu
